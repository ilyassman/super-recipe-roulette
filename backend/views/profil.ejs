<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Profil - Super Recipe Roulette</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/assets/css/profil.css">
</head>
<body>
    <!-- Header / Navigation -->
    <%- include('partials/navbar', { activePage: 'profil', logged: logged }) %>

    <!-- Main Content -->
    <main class="profil-page">
        <div class="container py-5">
            <!-- En-tête du profil -->
            <div class="profil-header">
                <div class="profil-avatar">
                    <i class="bi bi-person-circle"></i>
                </div>
                <div class="profil-info">
                    <h1 class="profil-name"><%= userName || 'Utilisateur' %></h1>
                    <p class="profil-email">
                        <i class="bi bi-envelope me-2"></i><%= userEmail || 'email@example.com' %>
                    </p>
                    <span class="profil-role badge">
                        <i class="bi bi-shield-check me-1"></i><%= userRole || 'user' %>
                    </span>
                </div>
            </div>

            <!-- Section Favoris -->
            <div class="favoris-section">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="bi bi-heart-fill me-2 text-danger"></i>Mes Recettes Favorites
                    </h2>
                    <p class="section-subtitle">Vos recettes sauvegardées</p>
                </div>

                <!-- Liste des favoris (statique pour l'instant) -->
                <div class="favoris-list">
                    <!-- Favori 1 -->
                     <%if (favorites.length > 0) { %>
                     <% favorites.forEach((favorite) => { %>
                    <div class="favori-card">
                        <div class="favori-image">
                            <img src="/assets/img/<%= favorite.image %>" alt="Poulet rôti au citron et herbes">
                        </div>
                        <div class="favori-content">
                            <h3 class="favori-title"><%= favorite.titre %></h3>
                            <p class="favori-description"><%= favorite.description %></p>
                            <div class="favori-badges">
                                <span class="badge-categorie"><%= favorite.categorie %></span>
                                <span class="badge-temps"><i class="bi bi-clock"></i> <%= favorite.temps_preparation %> min</span>
                                <span class="badge-difficulte"><%= favorite.difficulte %></span>
                            </div>
                            <a href="/recette/<%= favorite.id %>" class="btn-view-recipe">
                                Voir la recette <i class="bi bi-arrow-right"></i>
                            </a>
                        </div>
                        <button class="btn-remove-favori" title="Retirer des favoris">
                            <i class="bi bi-heart-fill"></i>
                        </button>
                    </div>
                    <% }); %>
                    <% } else { %>

                    
                
                <!-- Message si aucun favori -->
                <div class="no-favoris ">
                    <div class="text-center py-5">
                        <i class="bi bi-heart fs-1 text-muted mb-3"></i>
                        <p class="text-muted">Vous n'avez pas encore de recettes favorites.</p>
                        <a href="/recherche" class="btn btn-primary mt-3">
                            <i class="bi bi-search me-2"></i>Découvrir des recettes
                        </a>
                    </div>
                </div>
                <% } %>
            </div>

            <!-- Bouton de déconnexion -->
            <div class="deconnexion-section">
                <button class="btn-deconnexion" id="btnDeconnexion">
                    <i class="bi bi-box-arrow-right me-2"></i>Se déconnecter
                </button>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    
    <script>
        // Gestion de la déconnexion
        $(function() {
            $('#btnDeconnexion').on('click', function() {
                if (confirm('Êtes-vous sûr de vouloir vous déconnecter ?')) {
                    window.location.href = '/logout';
                }
            });
        });
    </script>
</body>
</html>

